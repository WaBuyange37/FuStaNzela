let arr = [1,2,3,4,5]
console.log(move(arr,1,3));
function move(array, index, offset){
  // let temp;
  // temp= array[index];
  // array[index]= array[offset];
  // array[offset]=temp;  
  // return array
  
}
